

Format:

```js
{
  // Deploy the token on eSpace and grab the token contract address
  "address": "0x9e9141aFeC83d244eB50E2fe9db8039100ae2738",
  "name": "Swappi Token 6th",
  // Please also upload the icon image with .png as suffix in the icons folder.
  // Name it as <symbol>.png in lowercase. eg. 'ppi.icon'
  "symbol": "PPI",
  "decimals": 18,
  "description": "PPI is an algorithmic, autonomous interest rate protocol built for developers, to unlock a universe of open financial applications."
  // An optional `id` can be used when the same ido token need auction more then once.
  "id": 2, // Optional, default: 1
}
```
